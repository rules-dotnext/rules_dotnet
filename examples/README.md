# Examples

Most subdirectories here are internal integration tests for rules_dotnet development.

For a **copy-pasteable starter project**, see [`nuget_hello/`](nuget_hello/) — it demonstrates `csharp_library` with a NuGet dependency, `csharp_binary`, and `csharp_nunit_test` in a standalone workspace.
