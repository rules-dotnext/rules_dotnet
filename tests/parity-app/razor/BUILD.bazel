"""spec-razor-blazor targets — Razor component compilation.

Uses stub rule until spec-razor-blazor is implemented (#249).
"""

load("@bazel_skylib//rules:build_test.bzl", "build_test")
load("//:stubs.bzl", "razor_library")

# BEFORE FIX: stub genrule fails with "razor_library is not yet implemented"
razor_library(
    name = "razor_components",
    razor_srcs = ["//:Counter.razor"],
    target_frameworks = ["net8.0"],
)

build_test(
    name = "build_test",
    targets = [":razor_components"],
)
