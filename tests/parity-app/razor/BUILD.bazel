"""spec-razor-blazor targets — Razor component compilation (#249)."""

load("@bazel_skylib//rules:build_test.bzl", "build_test")
load("@rules_dotnet//dotnet:defs.bzl", "razor_library")

razor_library(
    name = "razor_components",
    razor_srcs = ["//:Counter.razor"],
    target_frameworks = ["net8.0"],
)

build_test(
    name = "build_test",
    targets = [":razor_components"],
)
