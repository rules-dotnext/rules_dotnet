"""spec-platform-features targets — .resx resource compilation.

Uses stub rules until spec-platform-features is implemented (#466).
"""

load("@bazel_skylib//rules:build_test.bzl", "build_test")
load("//:stubs.bzl", "resx_resource")

# --- #466: .resx resource compilation ---
# BEFORE FIX: stub genrule fails with "resx_resource is not yet implemented"
resx_resource(
    name = "greeter_resources",
    srcs = ["//:Greeter.resx"],
    visibility = ["//visibility:public"],
)

build_test(
    name = "build_test",
    targets = [":greeter_resources"],
)
