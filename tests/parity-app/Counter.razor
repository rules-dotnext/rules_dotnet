@* Minimal Razor component — exercises spec-razor-blazor #249. *@
@* If this compiles via razor_library, Razor source generation works. *@

<h1>Count: @count</h1>
<button @onclick="Increment">+1</button>

@code {
    private int count = 0;
    private void Increment() => count++;
}
